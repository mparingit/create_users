---
- name: Create the User
  user:
     name: "{{ username }}"
     comment: My very first user
     shell: /bin/bash
     groups: admins,developers
  when: install


- name: Delete the user
  user:
     name: "{{ username }}"
     state: absent
  when: not install
